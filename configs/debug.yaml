project:
  name: CLEAN
  root: /Users/a203/CLEAN

runtime:
  device: cpu
  debug: true
  seed: 42

paths:
  app_dir: app
  workspace_dir: workspace

  raw_dir: workspace/data/raw
  sample_dir: workspace/data/sample
  processed_dir: workspace/data/processed
  embedding_dir: workspace/data/embeddings
  retrieval_dir: workspace/data/retrieval

  output_dir: workspace/outputs
  log_dir: workspace/outputs/logs
  ckpt_dir: workspace/outputs/checkpoints
  pred_dir: workspace/outputs/predictions
  fig_dir: workspace/outputs/figures
  cache_dir: workspace/cache

data:
  train_file: workspace/data/sample/train_tiny.csv
  test_file: workspace/data/sample/test_tiny.csv
  fasta_file: workspace/data/sample/tiny.fasta
  embedding_file: workspace/data/embeddings/tiny_embeddings.pt

train:
  batch_size: 2
  num_workers: 0
  epochs: 1
  lr: 1e-4

retrieval:
  topk: 3
  use_faiss: false

experiment:
  name: clean_tiny_debug
  model_type: clean